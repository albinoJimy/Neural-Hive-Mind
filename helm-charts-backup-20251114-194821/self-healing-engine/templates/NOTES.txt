🛡️ Self-Healing Engine deployed successfully!

Release:    {{ .Release.Name }}
Namespace:  {{ .Release.Namespace }}
Chart:      {{ .Chart.Name }}-{{ .Chart.Version }}
App:        {{ .Chart.AppVersion }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 DEPLOYMENT STATUS

Check pod status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "self-healing-engine.name" . }}

View logs:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "self-healing-engine.name" . }} -f

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔗 ENDPOINTS

Port-forward to access locally:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "self-healing-engine.fullname" . }} 8080:8080 50051:50051 9090:9090

Available endpoints:
  • Health Check:     http://localhost:8080/health/live
  • Readiness Check:  http://localhost:8080/health/ready
  • Metrics:          http://localhost:9090/metrics
  • gRPC Service:     localhost:50051

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📈 MONITORING

{{- if .Values.serviceMonitor.enabled }}
✓ ServiceMonitor enabled - Prometheus scraping configured
{{- else }}
✗ ServiceMonitor disabled
{{- end }}

{{- if .Values.autoscaling.enabled }}
✓ Autoscaling enabled ({{ .Values.autoscaling.minReplicas }}-{{ .Values.autoscaling.maxReplicas }} replicas)
{{- else }}
✗ Autoscaling disabled (fixed {{ .Values.replicaCount }} replicas)
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚙️  CONFIGURATION

Service Name:      {{ .Values.config.serviceName }}
Namespace:         {{ .Values.config.namespace }}
Kafka:             {{ .Values.config.kafka.bootstrapServers }}
Service Registry:  {{ .Values.config.serviceRegistry.host }}:{{ .Values.config.serviceRegistry.port }}
MongoDB:           {{ .Values.config.mongodb.uri }}
Redis:             {{ .Values.config.redis.host }}:{{ .Values.config.redis.port }}
Log Level:         {{ .Values.config.observability.logLevel }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📖 PLAYBOOKS

Directory:         {{ .Values.config.playbooks.dir }}
Timeout:           {{ .Values.config.playbooks.timeoutSeconds }}s
Max Retries:       {{ .Values.config.playbooks.maxRetries }}
Validation:        {{ .Values.config.playbooks.validationEnabled }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔐 RBAC

{{- if .Values.rbac.create }}
✓ RBAC enabled - Service has necessary permissions for playbook execution
{{- else }}
⚠ RBAC disabled - Ensure ServiceAccount has required permissions
{{- end }}

The Self-Healing Engine requires permissions to:
  • Read/Update Pods, Deployments, Services
  • Execute remediation actions in {{ .Values.config.namespace }}

Verify permissions:
  kubectl auth can-i --list --as=system:serviceaccount:{{ .Release.Namespace }}:{{ include "self-healing-engine.serviceAccountName" . }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 NEXT STEPS

1. Verify the deployment is healthy:
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "self-healing-engine.name" . }}

2. Check application logs:
   kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "self-healing-engine.name" . }} --tail=100

3. Monitor metrics:
   kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "self-healing-engine.fullname" . }} 9090:9090
   curl http://localhost:9090/metrics

4. Test remediation (trigger a sample failure scenario)

5. View traces in Jaeger (if configured):
   Check OpenTelemetry endpoint: {{ .Values.config.observability.otelEndpoint }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 DOCUMENTATION

For local development, use:
  helm install self-healing-engine ./helm-charts/self-healing-engine -f values-local.yaml

For production deployment:
  helm install self-healing-engine ./helm-charts/self-healing-engine

Troubleshooting:
  kubectl describe pod -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "self-healing-engine.name" . }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🗑️  UNINSTALL

To remove this deployment:
  helm uninstall {{ .Release.Name }} -n {{ .Release.Namespace }}

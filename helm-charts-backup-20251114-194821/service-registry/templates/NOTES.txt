🎉 Service Registry implantado com sucesso!

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Chart Version: {{ .Chart.Version }}
App Version: {{ .Chart.AppVersion }}

═══════════════════════════════════════════════════════════════

📊 STATUS DO DEPLOYMENT

Verificar pods:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "service-registry.name" . }}

Logs:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "service-registry.name" . }} -f

═══════════════════════════════════════════════════════════════

🌐 ENDPOINTS DISPONÍVEIS

Port-forward para acesso local:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "service-registry.fullname" . }} 50051:50051 9090:9090

⚠️  NOTA: Service Registry usa gRPC, não HTTP REST

Endpoints:
  • gRPC:          localhost:50051
  • Métricas:      http://localhost:9090/metrics

═══════════════════════════════════════════════════════════════

📈 MONITORAMENTO

ServiceMonitor: {{ if .Values.serviceMonitor.enabled }}✓ Habilitado{{ else }}✗ Desabilitado{{ end }}
{{- if .Values.autoscaling.enabled }}
Autoscaling: ✓ Habilitado ({{ .Values.autoscaling.minReplicas }}-{{ .Values.autoscaling.maxReplicas }} réplicas)
{{- else }}
Réplicas: {{ .Values.replicaCount }} (fixo)
{{- end }}
{{- if .Values.podDisruptionBudget }}
PodDisruptionBudget: ✓ Habilitado (minAvailable: {{ .Values.podDisruptionBudget.minAvailable }})
{{- end }}
{{- if .Values.networkPolicy.enabled }}
NetworkPolicy: ✓ Habilitado
{{- end }}

═══════════════════════════════════════════════════════════════

⚙️  CONFIGURAÇÃO

Service Name: {{ .Values.config.serviceName }}
Environment: {{ .Values.config.environment }}
Log Level: {{ .Values.config.logLevel }}

Etcd:
  • Endpoints: {{ .Values.config.etcd.endpoints | join ", " }}
  • Prefix: {{ .Values.config.etcd.prefix }}
  • Timeout: {{ .Values.config.etcd.timeoutSeconds }}s

Redis Cluster:
  • Nodes: {{ .Values.config.redis.clusterNodes | join ", " }}

═══════════════════════════════════════════════════════════════

❤️  HEALTH CHECK & HEARTBEAT

Health Check:
  • Interval: {{ .Values.config.healthCheck.intervalSeconds }}s

Heartbeat:
  • Timeout: {{ .Values.config.heartbeat.timeoutSeconds }}s

═══════════════════════════════════════════════════════════════

🔒 ISTIO mTLS

Modo: {{ .Values.istio.mtls.mode }}

═══════════════════════════════════════════════════════════════

🚀 PRÓXIMOS PASSOS

1. Verificar registro de agentes no etcd:
   kubectl exec -it -n {{ .Release.Namespace }} deploy/etcd -- \
     etcdctl get {{ .Values.config.etcd.prefix }} --prefix

2. Testar health checks:
   kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "service-registry.name" . }} | grep "health"

3. Validar heartbeats:
   kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "service-registry.name" . }} | grep "heartbeat"

4. Verificar métricas de registro/descoberta:
   curl http://localhost:9090/metrics | grep service_registry

5. Testar conectividade gRPC (se grpcurl estiver disponível):
   grpcurl -plaintext localhost:50051 list
   grpcurl -plaintext localhost:50051 describe

═══════════════════════════════════════════════════════════════

💻 AMBIENTE LOCAL

Em ambiente local (Minikube/Kind/Docker Desktop), alguns recursos podem
estar desabilitados para compatibilidade:
  • NetworkPolicy (networkPolicy.enabled: false)
  • ServiceMonitor (serviceMonitor.enabled: false)
  • Istio mTLS pode precisar ser ajustado para PERMISSIVE

═══════════════════════════════════════════════════════════════

🗑️  DESINSTALAÇÃO

Para remover este release:
  helm uninstall {{ .Release.Name }} -n {{ .Release.Namespace }}

✅ Execution Ticket Service successfully deployed!

Release Details:
  Name: {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}
  Chart: {{ .Chart.Name }}-{{ .Chart.Version }}
  App Version: {{ .Chart.AppVersion }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 Deployment Status

Check pod status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "execution-ticket-service.name" . }}

View logs:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "execution-ticket-service.name" . }} -f

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🌐 Endpoints

Service Type: {{ .Values.service.type }}

For local access, use port-forward:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "execution-ticket-service.fullname" . }} 8000:8000 9090:9090 50052:50052

Available endpoints:
  • Health Check:  http://localhost:8000/health
  • Readiness:     http://localhost:8000/ready
  • Metrics:       http://localhost:9090/metrics
  • REST API:      http://localhost:8000/api/v1/
  • gRPC:          localhost:50052

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📈 Monitoring

{{- if .Values.serviceMonitor.enabled }}
  ✅ ServiceMonitor: Enabled (Prometheus will scrape metrics)
{{- else }}
  ⚠️  ServiceMonitor: Disabled (enable for production monitoring)
{{- end }}

{{- if .Values.autoscaling.enabled }}
  ✅ Autoscaling: Enabled ({{ .Values.autoscaling.minReplicas }}-{{ .Values.autoscaling.maxReplicas }} replicas)
{{- else }}
  ℹ️  Autoscaling: Disabled (fixed {{ .Values.replicaCount }} replicas)
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚙️  Configuration

  PostgreSQL:  {{ .Values.config.postgres.host }}:{{ .Values.config.postgres.port }}
  MongoDB:     {{ .Values.config.mongodb.database }}
  Kafka:       {{ .Values.config.kafka.bootstrapServers }}
  Environment: {{ .Values.config.environment }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 Next Steps

{{- if eq .Values.config.environment "local" }}

For Local Development:
  1. Test the health endpoint:
     curl http://localhost:8000/health

  2. Check logs for startup messages:
     kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "execution-ticket-service.name" . }} -f

  3. Test gRPC endpoints using grpcurl or your gRPC client

{{- else }}

For Production:
  1. Verify metrics in Grafana:
     Check dashboards for execution-ticket-service

  2. Monitor traces in Jaeger:
     Verify distributed tracing is working

  3. Check PodDisruptionBudget status:
     kubectl get pdb -n {{ .Release.Namespace }}

  4. Verify autoscaling (if enabled):
     kubectl get hpa -n {{ .Release.Namespace }}

{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🗑️  Uninstall

To remove this release:
  helm uninstall {{ .Release.Name }} -n {{ .Release.Namespace }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

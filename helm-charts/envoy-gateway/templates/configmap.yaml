apiVersion: v1
kind: ConfigMap
metadata:
  name: envoy-gateway-config
  namespace: {{ .Release.Namespace }}
  labels:
    app: envoy-gateway
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
data:
  envoy-specialists-gateway.yaml: |
{{ .Files.Get "../../infrastructure/envoy/envoy-specialists-gateway.yaml" | indent 4 }}
  ratelimit-config.yaml: |
{{ .Files.Get "../../infrastructure/envoy/ratelimit-config.yaml" | indent 4 }}

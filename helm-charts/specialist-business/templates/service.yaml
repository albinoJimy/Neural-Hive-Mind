apiVersion: v1
kind: Service
metadata:
  name: {{ include "specialist-business.fullname" . }}
  labels:
    {{- include "specialist-business.labels" . | nindent 4 }}
    neural.hive/metrics: "enabled"
    neural.hive/component: "business-specialist"
    neural.hive/layer: "cognitiva"
  annotations:
    neural-hive.io/component: business-specialist
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.ports.grpc.port }}
      targetPort: grpc
      protocol: {{ .Values.service.ports.grpc.protocol }}
      name: grpc
    - port: {{ .Values.service.ports.http.port }}
      targetPort: http
      protocol: {{ .Values.service.ports.http.protocol }}
      name: http
    - port: {{ .Values.service.ports.metrics.port }}
      targetPort: metrics
      protocol: {{ .Values.service.ports.metrics.protocol }}
      name: metrics
  selector:
    {{- include "specialist-business.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "knowledge-graph-rag.fullname" . }}
  labels:
    {{- include "knowledge-graph-rag.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "knowledge-graph-rag.selectorLabels" . | nindent 4 }}

🎯 Consensus Engine deployed successfully!

Release:    {{ .Release.Name }}
Namespace:  {{ .Release.Namespace }}
Chart:      {{ .Chart.Name }}-{{ .Chart.Version }}
App:        {{ .Chart.AppVersion }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 DEPLOYMENT STATUS

Check pod status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "consensus-engine.name" . }}

View logs:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "consensus-engine.name" . }} -f

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔗 ENDPOINTS

Port-forward to access locally:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "consensus-engine.fullname" . }} 8000:8000 8080:8080

Available endpoints:
  • Health Check:     http://localhost:8000/health
  • Readiness Check:  http://localhost:8000/ready
  • Metrics:          http://localhost:8080/metrics

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📈 MONITORING

{{- if .Values.serviceMonitor.enabled }}
✓ ServiceMonitor enabled - Prometheus scraping configured
{{- else }}
✗ ServiceMonitor disabled
{{- end }}

{{- if .Values.autoscaling.enabled }}
✓ Autoscaling enabled ({{ .Values.autoscaling.minReplicas }}-{{ .Values.autoscaling.maxReplicas }} replicas)
{{- else }}
✗ Autoscaling disabled (fixed {{ .Values.replicaCount }} replicas)
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚙️  CONFIGURATION

Environment:  {{ .Values.config.environment }}
Kafka:        {{ .Values.config.kafka.bootstrapServers }}
MongoDB:      {{ .Values.config.mongodb.uri }}
Redis:        {{ .Values.config.redis.clusterNodes }}
Specialists:  gRPC endpoints configured
Log Level:    {{ .Values.config.logLevel }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 NEXT STEPS

1. Verify the deployment is healthy:
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "consensus-engine.name" . }}

2. Check application logs:
   kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "consensus-engine.name" . }} --tail=100

3. Monitor metrics:
   kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "consensus-engine.fullname" . }} 8080:8080
   curl http://localhost:8080/metrics

4. View traces in Jaeger (if configured):
   Check OpenTelemetry endpoint: {{ .Values.config.openTelemetry.endpoint }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 DOCUMENTATION

For local development, use:
  helm install consensus-engine ./helm-charts/consensus-engine -f values-local.yaml

For production deployment:
  helm install consensus-engine ./helm-charts/consensus-engine

Troubleshooting:
  kubectl describe pod -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "consensus-engine.name" . }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🗑️  UNINSTALL

To remove this deployment:
  helm uninstall {{ .Release.Name }} -n {{ .Release.Namespace }}

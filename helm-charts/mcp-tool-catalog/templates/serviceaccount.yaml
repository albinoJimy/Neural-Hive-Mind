{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mcp-tool-catalog.serviceAccountName" . }}
  labels:
    {{- include "mcp-tool-catalog.labels" . | nindent 4 }}
{{- end }}

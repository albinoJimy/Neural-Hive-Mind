🎉 MCP Tool Catalog implantado com sucesso!

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Chart Version: {{ .Chart.Version }}
App Version: {{ .Chart.AppVersion }}

═══════════════════════════════════════════════════════════════

📊 STATUS DO DEPLOYMENT

Verificar pods:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "mcp-tool-catalog.name" . }}

Logs:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "mcp-tool-catalog.name" . }} -f

═══════════════════════════════════════════════════════════════

🌐 ENDPOINTS DISPONÍVEIS

Port-forward para acesso local:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "mcp-tool-catalog.fullname" . }} 8080:8080 9091:9091

Endpoints:
  • Health Check:  http://localhost:8080/health
  • Readiness:     http://localhost:8080/ready
  • Métricas:      http://localhost:9091/metrics

Endpoints de API:
  • Catálogo:      GET http://localhost:8080/api/v1/tools
  • Seleção:       POST http://localhost:8080/api/v1/select
  • Execução:      POST http://localhost:8080/api/v1/execute

═══════════════════════════════════════════════════════════════

📈 MONITORAMENTO

ServiceMonitor: {{ if .Values.serviceMonitor.enabled }}✓ Habilitado{{ else }}✗ Desabilitado{{ end }}
{{- if .Values.autoscaling.enabled }}
Autoscaling: ✓ Habilitado ({{ .Values.autoscaling.minReplicas }}-{{ .Values.autoscaling.maxReplicas }} réplicas)
{{- else }}
Réplicas: {{ .Values.replicaCount }} (fixo)
{{- end }}
{{- if .Values.podDisruptionBudget }}
PodDisruptionBudget: ✓ Habilitado (minAvailable: {{ .Values.podDisruptionBudget.minAvailable }})
{{- end }}
{{- if .Values.networkPolicy.enabled }}
NetworkPolicy: ✓ Habilitado
{{- end }}

═══════════════════════════════════════════════════════════════

🧬 ALGORITMO GENÉTICO

Population Size: {{ .Values.geneticAlgorithm.populationSize }}
Max Generations: {{ .Values.geneticAlgorithm.maxGenerations }}
Crossover Probability: {{ .Values.geneticAlgorithm.crossoverProb }}
Mutation Probability: {{ .Values.geneticAlgorithm.mutationProb }}
Tournament Size: {{ .Values.geneticAlgorithm.tournamentSize }}
Convergence Threshold: {{ .Values.geneticAlgorithm.convergenceThreshold }}
Timeout: {{ .Values.geneticAlgorithm.timeout }}s

═══════════════════════════════════════════════════════════════

🔌 INTEGRAÇÕES

Kafka: {{ .Values.kafka.bootstrapServers }}
  • Request Topic: {{ .Values.kafka.requestTopic }}
  • Response Topic: {{ .Values.kafka.responseTopic }}
  • Consumer Group: {{ .Values.kafka.consumerGroupId }}

MongoDB: {{ .Values.mongodb.url }}
  • Database: {{ .Values.mongodb.database }}
  • Tools Collection: {{ .Values.mongodb.toolsCollection }}
  • Selections Collection: {{ .Values.mongodb.selectionsCollection }}

Redis: {{ .Values.redis.url }}
  • Cache TTL: {{ .Values.redis.cacheTtl }}s
  • Cache Enabled: {{ .Values.redis.cacheEnabled }}

Service Registry: {{ .Values.serviceRegistry.host }}:{{ .Values.serviceRegistry.port }}
  • Heartbeat Interval: {{ .Values.serviceRegistry.heartbeatInterval }}s

Analyst Agents: {{ .Values.analystAgents.host }}:{{ .Values.analystAgents.port }}

═══════════════════════════════════════════════════════════════

⚙️  EXECUÇÃO DE FERRAMENTAS

Timeout: {{ .Values.toolExecution.timeout }}s
Max Concurrent: {{ .Values.toolExecution.maxConcurrent }}
Max Retry Attempts: {{ .Values.toolExecution.retryMaxAttempts }}

═══════════════════════════════════════════════════════════════

🚀 PRÓXIMOS PASSOS

1. Testar endpoint de health:
   curl http://localhost:8080/health

2. Listar ferramentas disponíveis:
   curl http://localhost:8080/api/v1/tools

3. Testar seleção de ferramenta:
   curl -X POST http://localhost:8080/api/v1/select \
     -H "Content-Type: application/json" \
     -d '{"task": "analyze code quality", "context": {...}}'

4. Verificar histórico no MongoDB:
   kubectl exec -it -n {{ .Release.Namespace }} deploy/mongodb -- \
     mongosh {{ .Values.mongodb.database }} --eval "db.{{ .Values.mongodb.selectionsCollection }}.find().limit(5)"

5. Validar cache no Redis:
   kubectl exec -it -n {{ .Release.Namespace }} deploy/redis-cluster -- \
     redis-cli KEYS "*"

═══════════════════════════════════════════════════════════════

💻 AMBIENTE LOCAL

Em ambiente local (Minikube/Kind/Docker Desktop), alguns recursos podem
estar desabilitados para compatibilidade:
  • NetworkPolicy (networkPolicy.enabled: false)
  • ServiceMonitor (serviceMonitor.enabled: false)

═══════════════════════════════════════════════════════════════

🗑️  DESINSTALAÇÃO

Para remover este release:
  helm uninstall {{ .Release.Name }} -n {{ .Release.Namespace }}

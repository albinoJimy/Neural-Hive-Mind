apiVersion: v2
name: prometheus-pushgateway
description: Prometheus Pushgateway para métricas de CI/CD
type: application
version: 1.0.0
appVersion: "1.6.2"
keywords:
  - prometheus
  - pushgateway
  - metrics
  - observability
  - ci-cd
maintainers:
  - name: Neural Hive Mind Team
sources:
  - https://github.com/prometheus/pushgateway
dependencies:
  - name: prometheus-pushgateway
    version: "2.4.2"
    repository: "https://prometheus-community.github.io/helm-charts"
    condition: pushgateway.enabled

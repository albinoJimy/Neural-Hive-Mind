apiVersion: v1
kind: Secret
metadata:
  name: {{ include "service-registry.fullname" . }}-secret
  labels:
    {{- include "service-registry.labels" . | nindent 4 }}
type: Opaque
data:
  redis-password: {{ .Values.config.redis.password | b64enc | quote }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "specialist-architecture.fullname" . }}
  labels:
    {{- include "specialist-architecture.labels" . | nindent 4 }}
    neural.hive/metrics: "enabled"
    neural.hive/component: "architecture-specialist"
    neural.hive/layer: "cognitiva"
  annotations:
    neural-hive.io/component: business-specialist
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.ports.grpc.port }}
      targetPort: grpc
      protocol: {{ .Values.service.ports.grpc.protocol }}
      name: grpc
    - port: {{ .Values.service.ports.http.port }}
      targetPort: http
      protocol: {{ .Values.service.ports.http.protocol }}
      name: http
    - port: {{ .Values.service.ports.metrics.port }}
      targetPort: metrics
      protocol: {{ .Values.service.ports.metrics.protocol }}
      name: metrics
  selector:
    {{- include "specialist-architecture.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "orchestrator-dynamic.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "orchestrator-dynamic.labels" . | nindent 4 }}
automountServiceAccountToken: true

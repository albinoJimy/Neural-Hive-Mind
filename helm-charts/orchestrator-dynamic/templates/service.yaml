{{/*
Orchestrator Dynamic - Service usando templates comuns do Neural Hive Mind
*/}}
{{- $ctx := include "orchestrator-dynamic.context" . | fromYaml }}
{{- include "neural-hive.service" (dict "values" .Values "context" $ctx) }}

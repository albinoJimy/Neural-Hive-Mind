# LLM Configuration for Dataset Generation
# =========================================

# LLM Provider: openai, anthropic, local (Ollama)
LLM_PROVIDER=local

# API Keys (required for OpenAI/Anthropic)
LLM_API_KEY=

# Model Selection
# OpenAI: gpt-4, gpt-4-turbo, gpt-3.5-turbo
# Anthropic: claude-3-opus-20240229, claude-3-sonnet-20240229
# Ollama: llama2, mistral, codellama
LLM_MODEL=llama2

# Custom endpoint (optional, for local/custom LLM servers)
# Default for Ollama: http://ollama:11434/api
LLM_BASE_URL=http://ollama:11434/api

# Temperature for generation (0.0-1.0, higher = more creative)
LLM_TEMPERATURE=0.7

# Dataset Generation Configuration
# =================================

# Number of samples to generate per specialist
NUM_SAMPLES=1000

# Output directory for generated datasets
OUTPUT_DIR=/data/training

# Validate generated data against Avro schemas
VALIDATE_SCHEMAS=true

# Feature Extraction Configuration
# =================================

# Path to ontology files
ONTOLOGY_PATH=/app/ontologies

# Embeddings model for semantic analysis
EMBEDDINGS_MODEL=paraphrase-multilingual-MiniLM-L12-v2

# MLflow Configuration
# ====================

# MLflow tracking server
MLFLOW_TRACKING_URI=http://mlflow:5000

# MongoDB Configuration (for feedback data)
# ==========================================

MONGODB_URI=mongodb://mongodb.data-layer:27017
MONGODB_DB=neural_hive

# Model Training Configuration
# =============================

# Model type: random_forest, gradient_boosting, neural_network
MODEL_TYPE=random_forest

# Enable hyperparameter tuning (true/false)
HYPERPARAMETER_TUNING=false

# Auto-promote to Production if better than baseline (true/false)
PROMOTE_IF_BETTER=true

# Feedback window for retraining (days)
FEEDBACK_WINDOW_DAYS=30

# Minimum feedback quality to include (0.0-1.0)
MIN_FEEDBACK_QUALITY=0.5

# Training dataset path template
TRAINING_DATASET_PATH=/data/training/specialist_{specialist_type}_base.parquet

# Data Quality Validation
# =======================
DATA_QUALITY_MAX_MISSING_PCT=5.0
DATA_QUALITY_MAX_SPARSITY_PCT=50.0
DATA_QUALITY_MAX_OUTLIER_PCT=10.0
DATA_QUALITY_MIN_CLASS_PCT=5.0
DATA_QUALITY_MAX_CORRELATION=0.95
DATA_QUALITY_MIN_SCORE=0.6

# Core dependencies
python>=3.11

# ML and Data Processing
pandas==2.2.3
numpy==1.26.4
pyarrow>=14.0.0  # Para Parquet
scikit-learn==1.5.2

# LLM Clients
httpx==0.28.1
openai>=1.0.0  # Opcional, para OpenAI
anthropic>=0.7.0  # Opcional, para Anthropic

# Neural Hive Libraries
# Assumindo que neural_hive_specialists está instalado via pip install -e
# ou PYTHONPATH aponta para libraries/python

# Feature Extraction Dependencies
# torch 2.4.1 é requerido por sentence-transformers e compatível com transformers >= 4.32.0
torch==2.4.1
sentence-transformers==3.3.1  # Para embeddings
networkx>=3.0  # Para graph analysis

# Utilities
structlog==24.4.0
python-dotenv==1.0.1
tqdm>=4.66.0  # Progress bars

# Schema Validation
avro-python3>=1.10.0  # Para validar contra schemas Avro

# Testing (opcional)
pytest>=7.4.0
pytest-asyncio>=0.21.0

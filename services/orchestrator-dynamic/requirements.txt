# Framework web e servidor ASGI
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Temporal SDK para workflows e activities
temporalio==1.5.0

# Cliente Kafka com Schema Registry
confluent-kafka>=2.3.0
fastavro>=1.8.0
authlib>=1.3.0  # Required by confluent-kafka schema registry OAuth

# Driver PostgreSQL para state store Temporal
psycopg2-binary>=2.9.9

# Validação e configuração
pydantic==2.5.3
pydantic-settings>=2.1.0

# Logging estruturado
structlog==23.2.0

# Métricas Prometheus
prometheus-client>=0.19.0

# OpenTelemetry para tracing
opentelemetry-api>=1.21.0
opentelemetry-sdk>=1.21.0
opentelemetry-instrumentation-fastapi>=0.42b0
opentelemetry-instrumentation-kafka-python>=0.42b0
opentelemetry-exporter-otlp>=1.21.0

# Redis para cache e feromônios
redis[hiredis]==5.0.1

# MongoDB para auditoria
pymongo>=4.6.0
motor==3.3.2

# gRPC - versão consolidada 1.75.1+
grpcio>=1.75.1
grpcio-tools>=1.75.1
protobuf>=5.27.0

# Retry e resiliência
tenacity>=8.2.0
pybreaker>=1.0.0

# Cliente HTTP assíncrono para OPA e SLA Management System
aiohttp==3.9.1
httpx==0.25.2

# Cache LRU para decisões OPA
cachetools>=5.3.0

# Bibliotecas internas (instalar via path local ou registry privado)
# neural-hive-risk-scoring>=1.0.0
# Bibliotecas internas - usar caminhos do contexto de build (relativos a /build)
/libraries/neural_hive_integration
/libraries/security
/libraries/python/neural_hive_ml
/libraries/python/neural_hive_resilience
/libraries/python/neural_hive_observability

# ML and Data Science
scikit-learn>=1.5.0,<1.6.0
numpy==1.26.2
pandas==2.1.3
mlflow>=2.9.0
joblib>=1.3.0

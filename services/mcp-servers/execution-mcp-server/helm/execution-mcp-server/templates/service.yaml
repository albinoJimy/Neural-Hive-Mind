apiVersion: v1
kind: Service
metadata:
  name: execution-mcp-server
  labels:
    app: execution-mcp-server
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: execution-mcp-server

# Worker MCP Server - Environment de Teste

WORKER_MCP_SERVICE_NAME=worker-mcp-server
WORKER_MCP_SERVICE_VERSION=1.0.0
WORKER_MCP_LOG_LEVEL=DEBUG
WORKER_MCP_PORT=3013

# Worker Agents (mock em testes)
WORKER_MCP_WORKER_AGENT_HOST=localhost
WORKER_MCP_WORKER_AGENT_PORT=8005

# Orchestrator (mock em testes)
WORKER_MCP_ORCHESTRATOR_HOST=localhost
WORKER_MCP_ORCHESTRATOR_PORT=8003

# Service Registry (mock em testes)
WORKER_MCP_SERVICE_REGISTRY_HOST=localhost
WORKER_MCP_SERVICE_REGISTRY_PORT=8007

WORKER_MCP_EXECUTION_TIMEOUT=300

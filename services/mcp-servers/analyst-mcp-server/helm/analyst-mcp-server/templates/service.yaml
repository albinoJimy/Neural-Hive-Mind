apiVersion: v1
kind: Service
metadata:
  name: analyst-mcp-server
  labels:
    app: analyst-mcp-server
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: analyst-mcp-server

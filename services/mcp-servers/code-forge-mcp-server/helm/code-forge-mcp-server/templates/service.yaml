apiVersion: v1
kind: Service
metadata:
  name: {{ include "code-forge-mcp-server.fullname" . }}
  labels:
    app: {{ include "code-forge-mcp-server.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: mcp
    protocol: TCP
    name: mcp
  selector:
    app: {{ include "code-forge-mcp-server.name" . }}

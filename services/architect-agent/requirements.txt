# Dependências base consolidadas para Neural-Hive-Mind
-r ../../requirements-base.txt

# Service-specific dependencies
openai==1.10.0; python_version >= "3.12"
anthropic==0.18.0; python_version >= "3.12"

# NOVAS dependências para Fluxo G Fase 1
pyyaml>=6.0               # para parse Mermaid config
requests>=2.31             # para chamar APIs externas
click>=8.1                 # para CLI commands
graphviz>=0.20             # para geração de diagramas
tenacity>=8.2.0            # para retry logic com backoff
slowapi>=0.1.9            # para rate limiting


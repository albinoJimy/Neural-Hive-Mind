# ============================================================================
# Service Registry - Environment Configuration Template
# ============================================================================
# IMPORTANTE: Este arquivo e um TEMPLATE. Copie para .env e ajuste os valores.
# NUNCA commite o arquivo .env com credenciais reais no Git.
# ============================================================================

# Application
SERVICE_NAME=service-registry
SERVICE_VERSION=1.0.0
ENVIRONMENT=development  # development | staging | production
LOG_LEVEL=INFO

# Network
GRPC_PORT=50051
METRICS_PORT=9090

# Registry Backend (Redis)
# Nota: Mantemos nomes ETCD_* para compatibilidade, mas usa Redis
ETCD_ENDPOINTS=redis:6379
ETCD_PREFIX=neural-hive:agents
ETCD_TIMEOUT_SECONDS=5

# Health Checks
HEALTH_CHECK_INTERVAL_SECONDS=60
HEARTBEAT_TIMEOUT_SECONDS=120

# Redis (Pheromones)
REDIS_CLUSTER_NODES=redis:6379
REDIS_PASSWORD=CHANGEME_SECURE_PASSWORD  # Override para vazio apenas em DEV local. OBRIGATORIO em producao.

# Observability
OTEL_EXPORTER_ENDPOINT=http://otel-collector:4317

# Vault Integration
VAULT_ENABLED=false
VAULT_ADDRESS=http://vault.vault.svc.cluster.local:8200
VAULT_KUBERNETES_ROLE=service-registry
VAULT_TOKEN_PATH=/vault/secrets/token
VAULT_MOUNT_KV=secret

# SPIFFE Integration
SPIFFE_ENABLED=false
SPIFFE_SOCKET_PATH=unix:///run/spire/sockets/agent.sock
SPIFFE_TRUST_DOMAIN=neural-hive.local
SPIFFE_VERIFY_PEER=true

# ============================================================================
# NOTAS DE SEGURANCA PARA PRODUCAO
# ============================================================================
# 1. REDIS_PASSWORD - Obrigatorio em producao (validacao automatica)
# 2. Use External Secrets Operator ou Sealed Secrets para gerenciar credenciais
# 3. Habilite SPIFFE_ENABLED=true para mTLS entre servicos
# 4. Consulte docs/SECRETS_MANAGEMENT_GUIDE.md para melhores praticas
# ============================================================================

# PII Service Requirements

# Base (consolidado)
-r ../../requirements-base.txt

# Service-specific
# grpcio is now in requirements-base.txt
# protobuf is now in requirements-base.txt
pydantic-settings==2.6.1
structlog==24.4.0

# MongoDB para audit logging (INV-13)
motor==3.6.0
pymongo==4.10.1

# Criptografia para unmask reversível (INV-14)
cryptography==43.0.1

# Libraries Neural Hive-Mind
# Nota: Para Docker build, estas bibliotecas são copiadas e instaladas separadamente

# JWT auth
python-jose[cryptography]==3.3.0
pyjwt==2.10.1

# Grafana dashboard provisioning configuration

apiVersion: 1

providers:
  - name: 'Fluxo G Dashboards'
    orgId: 1
    folder: 'Fluxo G'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: true

# Core
pydantic>=2.5.2
pydantic-settings>=2.0.0
structlog>=23.2.0
aiohttp>=3.9.0

# NLP
spacy>=3.7.0

# gRPC
grpcio>=1.75.1
grpcio-tools>=1.75.1  # Version 1.75.1+ suporta protobuf 5.x / protoc 6.x (baseline nos serviços)
grpcio-health-checking>=1.75.1
protobuf>=5.27.0  # Supports runtime_version (needed for protoc 6.x generated code)

# MLflow
mlflow>=2.9.0

# MongoDB
pymongo>=4.6.0
motor>=3.3.0

# Redis
redis[hiredis]>=5.0.1

# Neo4j
neo4j>=5.15.0

# Observabilidade
opentelemetry-api>=1.21.0
opentelemetry-sdk>=1.21.0
opentelemetry-instrumentation-grpc>=0.42b0
opentelemetry-exporter-otlp-proto-grpc>=1.21.0
prometheus-client>=0.19.0

# Explicabilidade (opcional - apenas para análise avançada)
shap>=0.44.0
lime>=0.2.0.1

# Utilities
tenacity>=8.2.3
circuitbreaker>=1.4.0
python-json-logger>=2.0.7
networkx>=3.0

# Feature Extraction
sentence-transformers>=2.3.1

# Drift Monitoring (opcional - apenas para monitoramento de drift)
evidently>=0.4.0

# ML/Data Science
# NOTA: scikit-learn 1.5.x é a versão de referência para treinamento de modelos.
# Modelos treinados com sklearn 1.3.x tinham parâmetro monotonic_cst que foi removido em 1.5.x.
# Todos os modelos em Production devem ser retreinados com sklearn 1.5.x.
# O mlflow_client.py possui fallback joblib apenas para compatibilidade legada.
scikit-learn>=1.5.0,<1.6.0
pandas>=2.0.0
numpy>=1.24.0

# Business Metrics & Anomaly Detection
joblib>=1.3.0  # Para persistência de modelos Isolation Forest
clickhouse-driver>=0.2.6  # Opcional - apenas se usar ClickHouse

# Statistical Analysis for A/B Testing
scipy>=1.11.0  # Para chi-square test, t-test, Mann-Whitney U
statsmodels>=0.14.0  # Opcional - apenas para análise estatística avançada

# JWT Authentication
PyJWT>=2.8.0
cryptography>=41.0.0  # Para suporte a algoritmos RS256 (futuro)

# Compliance & Data Governance
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
# APScheduler>=3.10.0  # Opcional: para retention policies automáticas

# Neural Hive dependencies
# neuralHiveObservability @ file:///app/libraries/python/neural_hive_observability

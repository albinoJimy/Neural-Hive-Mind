# Core
pydantic==2.10.4
pydantic-settings==2.7.1
structlog==24.4.0
aiohttp==3.11.11

# NLP
spacy>=3.7.0

# gRPC
grpcio==1.68.1
grpcio-tools==1.68.1  # Version 1.75.1+ suporta protobuf==5.29.2 5.x / protoc 6.x (baseline nos serviços)
grpcio-health-checking==1.68.1
protobuf==5.29.2  # Supports runtime_version (needed for protoc 6.x generated code)

# MLflow
mlflow==2.19.0

# MongoDB
pymongo==4.10.1
motor==3.7.1

# Redis
redis[hiredis]==5.2.1

# Neo4j
neo4j>=5.15.0

# Observabilidade
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0
opentelemetry-instrumentation-grpc==0.50b0
opentelemetry-exporter-otlp-proto-grpc==1.29.0
prometheus-client==0.21.1

# Utilities
tenacity==9.0.0
circuitbreaker>=1.4.0
python-json-logger>=2.0.7
networkx>=3.0

# ML/Data Science
# NOTA: scikit-learn==1.6.0 1.5.x é a versão de referência para modelos em Production.
# Modelos foram retreinados com sklearn 1.5.2 em 2025-12-06 e promovidos ao MLflow.
# Compatível com sklearn 1.5.x (parâmetro monotonic_cst foi removido na migração).
scikit-learn==1.5.2
pandas==2.2.3
numpy==1.26.4

# Business Metrics & Anomaly Detection
joblib>=1.3.0  # Para persistência de modelos Isolation Forest
clickhouse-driver>=0.2.6  # Opcional - apenas se usar ClickHouse

# Statistical Analysis for A/B Testing
scipy==1.14.1  # Para chi-square test, t-test, Mann-Whitney U

# JWT Authentication
PyJWT>=2.8.0
cryptography>=41.0.0  # Para suporte a algoritmos RS256 (futuro)

# ============================================================================
# DEPENDÊNCIAS OPCIONAIS (não incluídas por padrão)
# ============================================================================
# As seguintes bibliotecas possuem lazy imports e podem ser instaladas
# opcionalmente se os módulos correspondentes forem necessários:
#
# Explicabilidade de modelos:
#   - shap>=0.44.0          (usado em explainability/shap_explainer.py)
#   - lime>=0.2.0.1         (usado em explainability/lime_explainer.py)
#
# Monitoramento de drift:
#   - evidently>=0.4.0      (usado em drift_monitoring/evidently_monitor.py)
#
# Análise semântica e embeddings:
#   - sentence-transformers==3.3.1  (usado em semantic_pipeline/ e feature_extraction/)
#
# Compliance e PII:
#   - presidio-analyzer>=2.2.0      (não usado atualmente)
#   - presidio-anonymizer>=2.2.0    (não usado atualmente)
#
# Análise estatística avançada:
#   - statsmodels>=0.14.0   (mover para optimizer-agents se necessário)
#
# Para instalar: pip install shap lime evidently sentence-transformers==3.3.1
# ============================================================================
# Neural Hive dependencies
# neuralHiveObservability @ file:///app/libraries/python/neural_hive_observability
